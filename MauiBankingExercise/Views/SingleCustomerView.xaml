<?xml version="1.0" encoding="utf-8" ?>
<views:BasePage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiBankingExercise.Views.SingleCustomerView"
             xmlns:viewmodels="clr-namespace:MauiBankingExercise.ViewModels"
             xmlns:views="clr-namespace:MauiBankingExercise.Views"
             xmlns:models="clr-namespace:MauiBankingExercise.Models"
             Title="{Binding Customer.FirstName}" x:DataType="viewmodels:SingleCustomerViewModel">
    <ScrollView>
        <StackLayout Padding="20">
            <Label Text="{Binding Customer.FirstName, StringFormat='Welcome, {0}'}"
                   FontAttributes="Bold" 
                   Margin="0,0,0,10"
                   FontSize="24"
                   HorizontalOptions="Center" />

            <Label Text="{Binding Customer.LastName}"
                   FontAttributes="Bold"
                   Margin="0,0,0,20"
                   FontSize="20"
                   HorizontalOptions="Center" />

        </StackLayout>
    </ScrollView>
  
</views:BasePage>