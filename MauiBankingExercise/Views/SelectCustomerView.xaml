<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:MauiBankingExercise.Models"
             x:Class="MauiBankingExercise.Views.SelectCustomerView"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             Title="Customers">
    <ContentPage.Behaviors>
        <toolkit:StatusBarBehavior StatusBarColor="Black" StatusBarStyle="LightContent" />
    </ContentPage.Behaviors>

    <ContentPage.Resources>
        <Style TargetType="Grid">
            <Setter Property="VisualStateManager.VisualStateGroups">
                <VisualStateGroupList>
                    <VisualStateGroup x:Name="CommonStates">
                        <VisualState x:Name="Normal" />
                        <VisualState x:Name="Selected">
                            <VisualState.Setters>
                                <Setter Property="BackgroundColor"
                                      Value="LightSkyBlue" />
                            </VisualState.Setters>
                        </VisualState>
                    </VisualStateGroup>
                </VisualStateGroupList>
            </Setter>
        </Style>
    </ContentPage.Resources>

    <CollectionView ItemsSource="{Binding Customers}" Margin="10" ItemSizingStrategy="MeasureAllItems"
                    SelectionMode="Single" x:Name="AllCustomersCollectionView"
                    SelectionChangedCommand="{Binding CustomerSelectedCommand}"
                    SelectionChangedCommandParameter="{Binding Path=SelectedItem, Source={x:Reference AllCustomersCollectionView}}">
        <CollectionView.ItemTemplate>
            <DataTemplate x:DataType="models:Customer">
                <Grid Padding="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Label Text="{Binding FirstName}"/>
                </Grid>
            </DataTemplate>
        </CollectionView.ItemTemplate>    
        
    </CollectionView>
    
    
</ContentPage>